{% set name = "ipywidgets-bokeh" %}
{% set version = "1.4.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/ipywidgets_bokeh-{{ version }}.tar.gz
  sha256: a62d528408e7f33ebc9d3c78361c08b72ace5aeb98df10986c1d1a5175433d56

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - pip
    - python >=3.7
  run:
    - python >=3.7
    - bokeh >=2.0.0
    - ipywidgets >=7.5

test:
  imports:
    - ipywidgets_bokeh
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/bokeh/ipywidgets_bokeh
  summary: Allows embedding of Jupyter widgets in Bokeh layouts.
  license: BSD-3-Clause
  license_file: LICENSE.txt

extra:
  recipe-maintainers:
    - raybellwaves
